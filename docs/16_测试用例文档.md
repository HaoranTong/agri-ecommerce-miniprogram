### 3.2 工具函数测试

当前已有测试：

```text
src/utils/logger.test.ts
src/utils/errorHandler.test.ts
```

后续新增工具函数时补充对应测试文件，并按以下规则命名：

```
{functionName}_{input}_{expected}
```
**示例**：

```
calculateDiscount_amount100_rate50_expected50  // 计算折扣-输入100-预期50
validatePhone_validNumber_expectedTrue         // 验证手机号-有效号码-预期真
```

### 1.3 测试用例优先级

| 优先级 | 说明 | 执行时机 |
|--------|------|----------|
| **P0** | 核心功能，必测 | 每次提交必执行 |
| **P1** | 重要功能 | 每日执行 |
| **P2** | 一般功能 | 每周执行 |
| **P3** | 边缘场景 | 发布前执行 |

### 1.4 测试数据管理

| 数据类型 | 管理方式 | 示例 |
|----------|----------|------|
| **测试用户** | 专用测试账号 | test001, test002 |
| **测试商品** | 固定 SKU | 大米 5kg（ID: 101） |
| **测试订单** | 自动生成后清理 | ORDER_TEST_001 |
| **测试积分** | 测试环境专用 | 固定积分池 |
| **测试优惠券** | 预生成有效券码 | TEST_COUPON_001 |

---

## 二、Postman 自动化测试

> 说明：接口路径/字段/错误码/状态枚举以 API 契约为唯一来源，详见 `docs/08_API_CONTRACT_V2.3.md`。

### 2.1 环境配置

#### 2.1.1 环境变量

```json
// MyShop_Test.postman_environment.json
{
  "id": "test-env-id",
  "name": "MyShop Test Environment",
  "values": [
    {
      "key": "baseUrl",
      "value": "https://agri-ecommerce.test/wp-json/myshop/v1",
      "type": "text"
    },
    {
      "key": "token",
      "value": "",
      "type": "secret"
    },
    {
      "key": "userId",
      "value": "",
      "type": "text"
    },
    {
      "key": "testPhone",
      "value": "13800138001",
      "type": "text"
    },
    {
      "key": "orderId",
      "value": "",
      "type": "text"
    }
  ]
}
```

#### 2.1.2 预请求脚本

```javascript
// pre-request script - 自动获取 token
if (!pm.environment.get('token')) {
  // 需要先登录获取 token
  console.log('请先设置 token 环境变量');
}
```

#### 2.1.3 测试脚本

```javascript
// testscript - 通用响应验证
pm.test('Response status is 200', () => {
  pm.response.to.have.status(200);
});

pm.test('Response success is true', () => {
  const jsonData = pm.response.json();
  pm.expect(jsonData.success).to.be.true;
});

// 数据验证
pm.test('Response has required fields', () => {
  const jsonData = pm.response.json();
  pm.expect(jsonData.data).to.have.property('user_id');
  pm.expect(jsonData.data).to.have.property('token');
});
```

### 2.2 认证模块测试用例

#### 2.2.1 登录接口

| 用例编号 | 用例名称 | 请求 | 预期结果 |
|----------|----------|------|----------|
| AUTH-001 | 正常登录 | 登录接口（见 API 契约） | 返回 token、user_id |
| AUTH-002 | 无效 code | 登录接口（见 API 契约） | 返回错误码 |
| AUTH-003 | 过期 code | 登录接口（见 API 契约） | 返回错误码 |
| AUTH-004 | 缺少参数 | 登录接口（见 API 契约） | 返回错误码（见 API 契约） |

#### 2.2.2 手机号绑定接口

| 用例编号 | 用例名称 | 请求 | 预期结果 |
|----------|----------|------|----------|
| AUTH-PHONE-001 | 绑定手机号成功 | 手机号绑定接口（见 API 契约） | 返回 phone |
| AUTH-PHONE-002 | 缺少参数 | 手机号绑定接口（见 API 契约） | 返回错误码（见 API 契约） |

**测试脚本**：

```javascript
// AUTH-001: 正常登录
pm.test('AUTH-001: 正常登录', () => {
  const jsonData = pm.response.json();
  pm.expect(jsonData.success).to.be.true;
  pm.expect(jsonData.data).to.have.property('token');
  pm.expect(jsonData.data).to.have.property('user_id');
  pm.expect(jsonData.data.token).to.not.be.empty;
  pm.environment.set('token', jsonData.data.token);
  pm.environment.set('userId', jsonData.data.user_id);
});

// AUTH-002: 无效 code
pm.test('AUTH-002: 无效 code', () => {
  const jsonData = pm.response.json();
  pm.expect(jsonData.success).to.be.false;
  pm.expect(jsonData.error_code).to.exist;
});
```

#### 2.2.3 用户资料接口

| 用例编号 | 用例名称 | 请求 | 预期结果 |
|----------|----------|------|----------|
| USER-001 | 获取资料 | 用户资料接口（见 API 契约） | 返回完整用户信息 |
| USER-002 | 更新昵称 | 用户资料更新接口（见 API 契约） | 更新成功 |
| USER-003 | 更新手机 | 用户资料更新接口（见 API 契约） | 格式正确则成功 |
| USER-004 | 非法手机 | 用户资料更新接口（见 API 契约） | 返回验证错误 |
| USER-005 | 获取地址 | 地址列表接口（见 API 契约） | 返回地址列表 |

### 2.3 订单模块测试用例

#### 2.3.1 创建订单

| 用例编号 | 用例名称 | 请求 | 预期结果 |
|----------|----------|------|----------|
| ORDER-001 | 正常创建 | 订单创建接口（见 API 契约） | 返回 order_id、状态字段（见 API 契约） |
| ORDER-002 | 积分抵扣 | 订单创建接口（见 API 契约） | 积分正确扣减 |
| ORDER-003 | 购物卡订单 | 订单创建接口（见 API 契约） | 生成购物卡 |
| ORDER-004 | 缺少参数 | 订单创建接口（见 API 契约） | 返回错误码（见 API 契约） |
| ORDER-005 | 库存不足 | 订单创建接口（见 API 契约） | 返回错误码（见 API 契约） |

**测试脚本**：

```javascript
// ORDER-001: 正常创建订单
pm.test('ORDER-001: 正常创建订单', () => {
  const jsonData = pm.response.json();
  pm.expect(jsonData.success).to.be.true;
  pm.expect(jsonData.data).to.have.property('order_id');
  pm.expect(jsonData.data.status).to.equal('pending');
  pm.environment.set('orderId', jsonData.data.order_id);
});

// ORDER-002: 积分抵扣订单
pm.test('ORDER-002: 积分抵扣订单', () => {
  const jsonData = pm.response.json();
  pm.expect(jsonData.data).to.have.property('points_discount');
  pm.expect(jsonData.data.points_used).to.equal(100);
});
```

#### 2.3.2 订单列表

| 用例编号 | 用例名称 | 请求 | 预期结果 |
|----------|----------|------|----------|
| ORDER-LIST-001 | 获取列表 | 订单列表接口（见 API 契约） | 返回订单数组 |
| ORDER-LIST-002 | 分页 | 订单列表接口（见 API 契约） | 返回分页数据 |
| ORDER-LIST-003 | 筛选状态 | 订单列表接口（见 API 契约） | 返回指定状态订单 |

#### 2.3.3 订单详情

| 用例编号 | 用例名称 | 请求 | 预期结果 |
|----------|----------|------|----------|
| ORDER-DETAIL-001 | 正常查询 | 订单详情接口（见 API 契约） | 返回完整订单信息 |
| ORDER-DETAIL-002 | 不存在订单 | 订单详情接口（见 API 契约） | 返回错误码（见 API 契约） |
| ORDER-DETAIL-003 | 申请退货 | 退货申请接口（见 API 契约） | 申请成功 |
| ORDER-DETAIL-004 | 退货图片上传 | 退货图片上传接口（见 API 契约） | 上传成功 |

### 2.4 购物卡模块测试用例

| 用例编号 | 用例名称 | 请求 | 预期结果 |
|----------|----------|------|----------|
| GC-LIST-001 | 获取列表 | 购物卡列表接口（见 API 契约） | 返回卡片数组 |
| GC-PURCHASE-001 | 购买储值卡 | 购卡接口（见 API 契约） | 生成 card_number |
| GC-SHARE-001 | 分享卡片 | 分享接口（见 API 契约） | 返回 share_token |
| GC-CLAIM-001 | 领取卡片 | 领取接口（见 API 契约） | 绑定到用户 |
| GC-REDEEM-001 | 兑换商品 | 兑换接口（见 API 契约） | 生成 0 元订单 |
| GC-STYLES-001 | 获取分享样式 | 分享样式接口（见 API 契约） | 返回样式列表 |

### 2.5 积分模块测试用例

| 用例编号 | 用例名称 | 请求 | 预期结果 |
|----------|----------|------|----------|
| POINTS-001 | 获取余额 | 积分余额接口（见 API 契约） | 返回余额信息 |
| POINTS-002 | 获取总览 | 积分总览接口（见 API 契约） | 返回总览与即将过期字段 |
| POINTS-003 | 获取流水 | 积分流水接口（见 API 契约） | 返回流水列表 |
| POINTS-004 | 兑换项列表 | 兑换项接口（见 API 契约） | 返回兑换项 |
| POINTS-005 | 积分兑换 | 积分兑换接口（见 API 契约） | 扣减积分并返回奖励 |
| POINTS-006 | 每日签到 | 签到接口（见 API 契约） | 成功领取积分 |
| POINTS-007 | 重复签到 | 签到接口（见 API 契约） | 返回重复签到错误（见 API 契约） |
| POINTS-008 | 获取规则 | 积分规则接口（见 API 契约） | 返回规则列表 |
| POINTS-009 | 任务列表 | 任务列表接口（见 API 契约） | 返回任务列表 |
| POINTS-010 | 领取奖励 | 任务领奖接口（见 API 契约） | 积分增加 |

### 2.6 优惠券模块测试用例

| 用例编号 | 用例名称 | 请求 | 预期结果 |
|----------|----------|------|----------|
| COUPON-001 | 验证有效券 | 优惠券校验接口（见 API 契约） | 返回券信息 |
| COUPON-002 | 验证无效券 | 优惠券校验接口（见 API 契约） | 返回错误 |
| COUPON-003 | 应用到订单 | 订单优惠券应用接口（见 API 契约） | 订单金额抵扣 |
| COUPON-004 | 重复使用 | 订单优惠券应用接口（见 API 契约） | 返回错误 |

### 2.7 分销模块测试用例

| 用例编号 | 用例名称 | 请求 | 预期结果 |
|----------|----------|------|----------|
| REFERRAL-001 | 邀请统计 | 邀请统计接口（见 API 契约） | 返回统计信息 |
| REFERRAL-002 | 下级列表 | 下级列表接口（见 API 契约） | 返回下级列表 |
| REFERRAL-003 | 渠道归因 | 渠道归因接口（见 API 契约） | 记录成功 |
| COMMISSION-001 | 佣金列表 | 佣金列表接口（见 API 契约） | 返回佣金流水 |
| COMMISSION-002 | 佣金总览 | 佣金总览接口（见 API 契约） | 返回各状态汇总 |
| COMMISSION-003 | 佣金提现申请 | 佣金提现接口（见 API 契约） | 返回提现申请记录 |
| COMMISSION-004 | 提现记录列表 | 提现记录接口（见 API 契约） | 返回提现记录列表 |

### 2.8 代理模块测试用例

| 用例编号 | 用例名称 | 请求 | 预期结果 |
|----------|----------|------|----------|
| AGENT-001 | 代理资料 | 代理资料接口（见 API 契约） | 返回代理信息 |
| AGENT-002 | 下级代理 | 下级代理接口（见 API 契约） | 返回下级列表 |
| AGENT-003 | 代理佣金 | 代理佣金接口（见 API 契约） | 返回佣金列表 |
| AGENT-004 | 代理申请 | 代理申请接口（见 API 契约） | 申请成功 |

---

## 三、单元测试

### 3.1 Jest 配置

```javascript
// jest.config.js
module.exports = {
  testEnvironment: 'jsdom',
  testMatch: ['**/__tests__/**/*.test.{ts,tsx}'],
  collectCoverageFrom: [
    'src/**/*.{ts,tsx}',
    '!src/**/*.d.ts',
    '!src/**/*.test.{ts,tsx}'
  ],
  coverageDirectory: 'coverage',
  coverageReporters: ['text', 'lcov', 'html'],
  moduleFileExtensions: ['ts', 'tsx', 'js', 'jsx'],
  transform: {
    '^.+\\.(ts|tsx)$': 'ts-jest'
  },
  setupFilesAfterEnv: ['<rootDir>/jest.setup.js'],
  testTimeout: 10000
};
```

### 3.2 工具函数测试

#### 3.2.1 手机号验证

```typescript
// src/utils/validator.test.ts
import { validatePhone } from '../utils/validator';

describe('validatePhone', () => {
  test('有效手机号', () => {
    expect(validatePhone('13800138000')).toBe(true);
    expect(validatePhone('15912345678')).toBe(true);
  });

  test('无效手机号 - 长度不足', () => {
    expect(validatePhone('1380013800')).toBe(false);
  });

  test('无效手机号 - 长度过长', () => {
    expect(validatePhone('138001380000')).toBe(false);
  });

  test('无效手机号 - 非数字', () => {
    expect(validatePhone('13800138abc')).toBe(false);
  });

  test('无效手机号 - 开头错误', () => {
    expect(validatePhone('12800138000')).toBe(false);
  });
});
```

#### 3.2.2 金额计算

```typescript
// src/utils/calculator.test.ts
import { calculatePointsDiscount, calculateOrderTotal } from '../utils/calculator';

describe('calculatePointsDiscount', () => {
  test('正常抵扣', () => {
    expect(calculatePointsDiscount(100, 50)).toBe(2); // 100积分，50积分=1元
  });

  test('积分不足', () => {
    expect(calculatePointsDiscount(50, 100)).toBe(1); // 50积分最多抵扣1元
  });

  test('抵扣超过订单金额', () => {
    expect(calculatePointsDiscount(500, 10, 30)).toBe(3); // 订单30元，最多抵扣30元（300积分）
  });
});
```

#### 3.2.3 订单状态映射

```typescript
// src/utils/order.test.ts
import { getOrderStatusText, getOrderStatusColor } from '../utils/order';

describe('order status helpers', () => {
  test('getOrderStatusText', () => {
    expect(getOrderStatusText('pending')).toBe('待支付');
    expect(getOrderStatusText('processing')).toBe('处理中');
    expect(getOrderStatusText('completed')).toBe('已完成');
    expect(getOrderStatusText('cancelled')).toBe('已取消');
  });

  test('getOrderStatusColor', () => {
    expect(getOrderStatusColor('pending')).toBe('#ff9900');
    expect(getOrderStatusColor('completed')).toBe('#52c41a');
    expect(getOrderStatusColor('cancelled')).toBe('#ff4d4f');
  });
});
```

### 3.3 Hooks 测试

#### 3.3.1 useCart Hook

```typescript
// src/hooks/useCart.test.ts
import { renderHook, act } from '@testing-library/react';
import { useCart } from '../hooks/useCart';
import { cartService } from '../services/api';

// Mock 服务
jest.mock('../services/api');

describe('useCart', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('初始状态', () => {
    const { result } = renderHook(() => useCart());
    expect(result.current.cartItems).toEqual([]);
    expect(result.current.loading).toBe(false);
  });

  test('加载购物车', async () => {
    const mockItems = [
      { product_id: 1, variation_id: 1, quantity: 2 }
    ];
    (cartService.getCart as jest.Mock).mockResolvedValue(mockItems);

    const { result, waitForNextUpdate } = renderHook(() => useCart());

    await waitForNextUpdate();

    expect(result.current.cartItems).toEqual(mockItems);
    expect(result.current.loading).toBe(false);
  });

  test('添加商品', async () => {
    (cartService.addToCart as jest.Mock).mockResolvedValue({ success: true });
    (cartService.getCart as jest.Mock).mockResolvedValue([]);

    const { result } = renderHook(() => useCart());

    await act(async () => {
      await result.current.addToCart(1, 2);
    });

    expect(cartService.addToCart).toHaveBeenCalledWith(1, 2);
  });
});
```

### 3.4 组件测试

#### 3.4.1 Button 组件

```typescript
// src/components/Button/Button.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { Button } from '../Button';

describe('Button', () => {
  test('渲染默认按钮', () => {
    render(<Button>点击我</Button>);
    expect(screen.getByText('点击我')).toBeInTheDocument();
  });

  test('渲染加载状态', () => {
    render(<Button loading>加载中</Button>);
    expect(screen.getByText('加载中')).toBeInTheDocument();
    expect(screen.getByRole('button')).toBeDisabled();
  });

  test('点击事件', () => {
    const onClick = jest.fn();
    render(<Button onClick={onClick}>点击我</Button>);
    fireEvent.click(screen.getByText('点击我'));
    expect(onClick).toHaveBeenCalledTimes(1);
  });

  test('禁用状态', () => {
    const onClick = jest.fn();
    render(<Button disabled onClick={onClick}>禁用</Button>);
    fireEvent.click(screen.getByText('禁用'));
    expect(onClick).not.toHaveBeenCalled();
  });
});
```

---

## 四、集成测试

### 4.1 购物流程测试

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录获取 token | token 有效 |
| 2 | 浏览商品列表 | 返回商品数据 |
| 3 | 添加商品到购物车 | 购物车数量+1 |
| 4 | 创建订单 | 订单创建成功 |
| 5 | 微信支付 | 支付成功，订单状态更新 |
| 6 | 支付页提示积分 | 若返回 `points_reward` 则显示提示 |
| 7 | 查看订单详情 | 显示完整信息 |

### 4.2 购物卡完整流程测试

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录 | token 有效 |
| 2 | 购买储值卡 | 生成 card_number |
| 3 | 分享购物卡 | 生成 share_token |
| 4 | 领取购物卡（另一用户） | 卡片绑定到新用户 |
| 5 | 使用购物卡支付订单 | 订单金额抵扣 |
| 6 | 查看余额 | 余额减少 |

### 4.3 积分完整流程测试

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录 | token 有效 |
| 2 | 创建订单（100元） | 获得 100 积分 |
| 3 | 再次下单使用积分 | 积分扣减 |
| 4 | 查看积分流水 | 显示获取和消费记录 |
| 5 | 查看积分余额 | 余额正确 |

---

## 五、测试数据准备

### 5.1 测试账号

| 账号 | 用途 | 积分余额 |
|------|------|----------|
| test001 | 常规测试 | 1000 |
| test002 | 积分测试 | 5000 |
| test003 | 购物卡测试 | 0 |
| test004 | 代理测试 | 0 |

### 5.2 测试商品

| 商品 ID | 名称 | 价格 | 库存 |
|---------|------|------|------|
| 101 | 五常稻花香大米 5kg | 58.00 | 100 |
| 102 | 五常稻花香大米 10kg | 88.00 | 50 |
| 103 | 珍珠米 5kg | 39.00 | 200 |

### 5.3 测试优惠券

| 券码 | 面额 | 有效期 | 状态 |
|------|------|--------|------|
| TEST10 | 10 元 | 30 天 | 有效 |
| TEST20 | 20 元 | 30 天 | 有效 |
| EXPIRED | 10 元 | 已过期 | 无效 |

### 5.4 测试购物卡

| 卡号 | 类型 | 面额 | 状态 |
|------|------|------|------|
| GC_TEST_001 | 储值卡 | 200 | 可用 |
| GC_TEST_002 | 固定组合 | 组合价 | 可用 |

---

## 六、测试执行规范

### 6.1 本地测试

```bash
# 安装依赖
npm install

# 运行单元测试
npm test

# 运行接口测试
npm run test:api

# 生成测试报告
npm run test:coverage
```

### 6.2 CI/CD 集成

```yaml
# .github/workflows/test.yml
name: Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install dependencies
        run: npm ci
      - name: Run unit tests
        run: npm test
      - name: Run API tests
        run: npm run test:api
        env:
          API_BASE: ${{ secrets.API_BASE }}
          API_TOKEN: ${{ secrets.API_TOKEN }}
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/lcov.info
```

### 6.3 测试报告

| 报告类型 | 生成方式 | 查看位置 |
|----------|----------|----------|
| 单元测试报告 | `npm test -- --coverage` | `coverage/index.html` |
| 接口测试报告 | `npm run test:api` | 控制台输出 + JUnit XML |
| 集成测试报告 | 手动执行 | Markdown 报告 |

---

## 七、缺陷管理

### 7.1 缺陷分级

| 级别 | 说明 | 响应时间 | 修复时间 |
|------|------|----------|----------|
| **P0 - 阻塞** | 核心功能不可用 | 立即 | 4 小时 |
| **P1 - 严重** | 主要功能异常 | 2 小时 | 1 天 |
| **P2 - 一般** | 次要功能异常 | 1 天 | 3 天 |
| **P3 - 轻微** | 体验问题 | 3 天 | 1 周 |

### 7.2 缺陷报告模板

```markdown
## 缺陷标题
[模块] 缺陷描述

## 环境信息
- 环境：开发/测试/生产
- 微信版本：8.0.x
- 小程序基础库：2.25.x

## 复现步骤
1. 打开小程序
2. 进入 XXX 页面
3. 执行 XXX 操作

## 预期结果
描述期望的行为

## 实际结果
描述实际发生的行为

## 截图/日志
[粘贴截图或日志]

## 严重程度
[P0/P1/P2/P3]
```

---

## 八、测试覆盖率要求

| 类型 | 最低要求 | 目标值 |
|------|----------|--------|
| 语句覆盖率 | 60% | 80% |
| 分支覆盖率 | 50% | 70% |
| 函数覆盖率 | 60% | 80% |
| 行覆盖率 | 60% | 80% |
| 接口测试覆盖率 | 70% | 90% |

---

> **文档结束**


