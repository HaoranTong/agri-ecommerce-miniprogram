# 代码审查与实施计划

> **生成时间**：2025-01-22  
> **目的**：全面理解前后端代码逻辑，制定详细的实施计划

---

## 📋 一、代码审查总结

### 1.1 购物卡分享样式系统

#### 当前实现状态
- ✅ **数据库字段**: `wp_myshop_gift_card_templates.share_template_config` (LONGTEXT)
- ✅ **默认模板**: `assets/giftcard/share-default.json`
- ✅ **后端解析**: `Gift_Card_Controller::decode_share_template_config()` 
- ✅ **分享时使用**: 分享时从模板读取配置，保存到 `share_meta` 中

#### 发现的问题
- ❌ **只有单个样式配置**: 目前每个模板只能配置一个分享样式
- ❌ **没有样式列表接口**: 缺少获取所有可用样式的API
- ❌ **没有占位样式**: 除了默认样式外，没有其他测试样式

#### 需要实现的功能
1. 创建多个占位分享样式模板（JSON文件）
2. 创建API接口：获取所有可用分享样式列表
3. 前端支持样式选择（预览图或样式名称列表）

---

### 1.2 支付流程系统

#### 当前实现状态
- ✅ **订单创建**: `Order_Controller::create()` 已完整实现
- ✅ **二维码提取**: 从 WooCommerce COD 网关的 `description` 字段提取图片URL
- ✅ **支付凭证上传**: `Order_Controller::upload_payment_proof()` 已完整实现
- ✅ **订单状态管理**: 上传凭证后订单状态变为 `processing`，等待后台手动确认

#### 支付流程逻辑
```
用户下单 → 创建订单 → 返回支付页面（显示收款码）
  → 用户扫码支付 → 上传付款凭证
  → 订单状态变为 processing
  → 后台管理员查看凭证 → 手动修改订单状态为 completed
```

#### 发现的功能缺失
- ❌ **优惠券功能**: 完全未实现，需要集成 WooCommerce 原生优惠券
- ❌ **购物卡支付**: 在支付页面使用储值购物卡的功能未实现
- ❌ **支付页面优化**: 缺少使用优惠券和购物卡的UI和逻辑

---

### 1.3 优惠券系统

#### 当前实现状态
- ❌ **完全未实现**: 代码库中没有任何优惠券相关代码
- ✅ **WooCommerce原生支持**: WordPress后台有优惠券管理功能

#### 需要实现的功能
1. 创建API接口：验证优惠券（`/coupons/validate`）
2. 创建API接口：应用优惠券到订单（`/orders/{id}/apply-coupon`）
3. 前端集成：在支付页面添加优惠券输入和验证

---

### 1.4 积分抵扣系统

#### 当前实现状态
- ✅ **订单创建支持**: `Order_Controller::create()` 已支持 `points_to_use` 参数
- ✅ **积分计算逻辑**: `calculate_points_discount()` 已实现
- ✅ **积分扣除**: `deduct_points_for_order()` 已实现
- ❌ **前端UI缺失**: 订单创建页面没有积分抵扣的UI

---

### 1.5 购物卡系统

#### 当前实现状态
- ✅ **购物卡创建**: `Gift_Card_Controller::purchase_card()` 已实现
- ✅ **购物卡分享**: `Gift_Card_Controller::share_card()` 已实现
- ✅ **购物卡列表**: `Gift_Card_Controller::list_cards()` 已实现
- ✅ **订单生成购物卡**: `handle_order_status_completed()` 已实现
- ❌ **购物卡支付**: 使用储值购物卡支付订单的功能未实现

---

## 📝 二、需要开发的功能清单

### 2.1 购物卡分享样式系统

#### 任务1：创建占位分享样式模板
**文件位置**: `wp-content/plugins/myshop-core/assets/giftcard/`
**需要创建的文件**:
1. `share-style-1.json` - 样式1（经典风格）
2. `share-style-2.json` - 样式2（简约风格）
3. `share-style-3.json` - 样式3（节日风格）

**每个样式文件结构**:
```json
{
  "id": "style-1",
  "name": "经典风格",
  "preview_image": "https://...",
  "title": "...",
  "subtitle": "...",
  "background_image": "...",
  "qr_caption": "...",
  "button_text": "...",
  "brand_logo": "...",
  "terms": [...]
}
```

#### 任务2：创建获取分享样式列表的API
**接口路径**: `GET /gift-cards/share-styles`
**功能**: 返回所有可用的分享样式列表

**实现位置**: `api/gift-card-controller.php`
```php
public static function list_share_styles($request) {
    // 读取 assets/giftcard/ 目录下的所有 share-*.json 文件
    // 返回样式列表（包含 id, name, preview_image 等）
}
```

#### 任务3：修改分享接口支持样式选择
**当前**: 分享时使用模板配置的单个样式
**改进**: 分享时可以选择多个可用样式之一

---

### 2.2 支付页面功能增强

#### 任务1：集成 WooCommerce 优惠券功能
**接口1**: `POST /coupons/validate`
- 功能: 验证优惠券号码
- 输入: `{ "code": "COUPON123" }`
- 输出: `{ "valid": true, "discount_amount": 10.00, "type": "percent|fixed" }`

**接口2**: `POST /orders/{order_id}/apply-coupon`
- 功能: 应用优惠券到订单
- 输入: `{ "coupon_code": "COUPON123" }`
- 输出: 更新后的订单信息（包含折扣后金额）

**实现要点**:
- 使用 WooCommerce 原生 `WC_Coupon` 类
- 验证优惠券是否有效、是否过期、是否已使用
- 计算折扣金额并应用到订单

#### 任务2：实现购物卡支付功能
**接口**: `POST /orders/{order_id}/apply-gift-card`
- 功能: 使用储值购物卡支付订单
- 输入: `{ "card_number": "GC20250122XXXXXX" }`
- 输出: 使用后的订单信息（包含购物卡使用金额、剩余金额）

**实现要点**:
- 验证购物卡是否存在且可用
- 验证购物卡持有人与订单用户一致
- 扣减购物卡余额
- 更新订单应付金额
- 如果余额足够，订单金额为0

#### 任务3：前端支付页面改造
**文件**: `src/pages/order/payment.tsx`
**需要添加的功能**:
1. "使用优惠券"按钮 → 弹出输入框 → 验证 → 显示折扣金额
2. "使用储值购物卡"按钮 → 弹出购物卡列表 → 选择 → 显示使用金额
3. 显示使用情况提醒（优惠券金额、购物卡使用金额、剩余金额、最终应付金额）

---

### 2.3 积分抵扣功能前端实现

#### 任务1：订单创建页面添加积分抵扣UI
**文件**: `src/pages/order/create.tsx`
**需要添加的功能**:
1. 显示用户可用积分余额
2. 输入框：使用积分数量
3. 自动计算：根据规则计算最大可用积分
4. 实时更新：显示抵扣后的订单金额
5. 前端校验：积分不足时自动填入全部可用积分

#### 任务2：积分兑换商品列表页面
**文件**: `src/pages/product/redeem.tsx` (新建)
**功能**: 只显示支持积分兑换的商品

---

### 2.4 购物卡相关优化

#### 任务1：统一称呼修改（礼品卡 → 购物卡）
- 修改所有页面文案
- 修改URL路径：`giftcard` → `shopping-card`
- 修改路由配置

#### 任务2：购物卡中心页面优化
- 统一功能按钮布局
- 参考用户中心的菜单风格

---

## 🔧 三、技术实施细节

### 3.1 优惠券API实现

```php
// 文件: api/coupon-controller.php (新建)

class Coupon_Controller {
    public static function register_routes() {
        register_rest_route('myshop/v1', '/coupons/validate', [
            'methods' => \WP_REST_Server::CREATABLE,
            'callback' => [self::class, 'validate_coupon'],
            'permission_callback' => ['MyShop_Auth', 'check_permission']
        ]);
        
        register_rest_route('myshop/v1', '/orders/(?P<order_id>\d+)/apply-coupon', [
            'methods' => \WP_REST_Server::CREATABLE,
            'callback' => [self::class, 'apply_coupon_to_order'],
            'permission_callback' => ['MyShop_Auth', 'check_permission']
        ]);
    }
    
    public static function validate_coupon($request) {
        $code = sanitize_text_field($request->get_param('code'));
        $coupon = new \WC_Coupon($code);
        
        if (!$coupon->get_id()) {
            return new WP_Error('coupon_not_found', '优惠券不存在', ['status' => 404]);
        }
        
        // 验证优惠券是否有效
        $is_valid = $coupon->is_valid();
        if (!$is_valid) {
            $errors = $coupon->get_errors();
            return new WP_Error('coupon_invalid', $errors ? implode(', ', $errors) : '优惠券无效', ['status' => 400]);
        }
        
        return rest_ensure_response([
            'success' => true,
            'data' => [
                'code' => $code,
                'discount_type' => $coupon->get_discount_type(),
                'amount' => $coupon->get_amount(),
                'description' => $coupon->get_description()
            ]
        ]);
    }
    
    public static function apply_coupon_to_order($request) {
        // 实现优惠券应用到订单的逻辑
    }
}
```

### 3.2 购物卡支付API实现

```php
// 在 Order_Controller 中添加新方法

public static function apply_gift_card_to_order($request) {
    $order_id = absint($request->get_param('order_id'));
    $card_number = sanitize_text_field($request->get_param('card_number'));
    
    // 1. 验证订单
    // 2. 验证购物卡
    // 3. 验证持有人
    // 4. 扣减余额
    // 5. 更新订单金额
    // 6. 返回结果
}
```

### 3.3 分享样式列表API实现

```php
// 在 Gift_Card_Controller 中添加新方法

public static function list_share_styles($request) {
    $styles_dir = MYSHOP_PLUGIN_DIR . 'assets/giftcard/';
    $styles = [];
    
    // 读取所有 share-*.json 文件
    $files = glob($styles_dir . 'share-*.json');
    foreach ($files as $file) {
        $content = file_get_contents($file);
        $style = json_decode($content, true);
        if ($style) {
            $styles[] = [
                'id' => $style['id'] ?? basename($file, '.json'),
                'name' => $style['name'] ?? '未命名样式',
                'preview_image' => $style['preview_image'] ?? '',
                'config' => $style
            ];
        }
    }
    
    return rest_ensure_response([
        'success' => true,
        'data' => $styles
    ]);
}
```

---

## 📅 四、开发优先级

### 优先级1：核心功能（立即开发）
1. ✅ 支付页面添加优惠券和购物卡功能
2. ✅ 订单创建页积分抵扣功能
3. ✅ 统一称呼和路径修改

### 优先级2：功能完善（本周内）
4. ✅ 积分兑换商品列表页面
5. ✅ 购物卡中心页面优化
6. ✅ 创建占位分享样式模板

### 优先级3：优化改进（后续）
7. ✅ 分享样式列表API和选择功能
8. ✅ 积分流水日期筛选
9. ✅ 其他优化

---

## ✅ 五、代码审查确认

### 已确认的实现
- ✅ 订单创建和支付流程完整
- ✅ 支付凭证上传功能完整
- ✅ 积分抵扣后端逻辑完整
- ✅ 购物卡创建、分享、管理功能完整
- ✅ 订单完成后自动生成购物卡功能完整

### 需要开发的功能
- ❌ 优惠券验证和应用功能
- ❌ 购物卡支付功能
- ❌ 积分抵扣前端UI
- ❌ 分享样式列表和选择
- ❌ 支付页面优惠券和购物卡UI

---

**下一步**: 按照优先级开始逐个功能开发。
