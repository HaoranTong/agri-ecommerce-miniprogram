# 工作状态冻结文档（对话上下文快照）

> 目的：在新对话窗口粘贴此文档后，AI 可 100% 还原当前工作状态、已完成内容与下一步待办事项。
> 适用范围：项目 `E:\projects\agri-ecommerce-miniprogram`（前端小程序 + 文档体系）。

---

## 一、项目目标与总要求（文档驱动）

- 目标：确保“文档驱动开发（DDD）”，所有定义/字段/接口/边界仅有一个“唯一出处（SSOT）”，其他文档只引用，不重复定义。
- 需求：统一文档编号并建立引用链路，保证无冲突、无重复、可由 AI 直接理解并完成开发/测试/执行。
- 重点关注：前端小程序模块边界、页面路由清单与 API 契约对齐；确保真实设备测试前文档/代码状态清晰。

---

## 二、已完成的关键工作（结论明确）

### 1. 文档编号与主索引
- 已创建 `docs/README.md` 作为“主索引/引用关系图”，明确 SSOT 与引用关系。
- `01_功能规格及技术架构说明书` 被设为基础核心文档。
- 文档编号逻辑：从需求基线 → 规格 → 模块边界 → 架构/数据/接口 → 规范/执行。

### 2. 新增唯一出处文档
- `docs/02_模块与边界定义_v1.0.md`：模块边界唯一出处。
- `docs/02A_术语与名词表_v1.0.md`：术语与名词唯一出处。
- `docs/02B_非功能需求与验收标准_v1.0.md`：非功能需求唯一出处。

### 3. 文档引用清理
已移除文档间重复定义（如错误码/状态枚举/字段定义），统一引用：
- 错误码/接口 → `docs/08_API_CONTRACT_V2.3.md`
- 字段定义 → `docs/06_DATA_DICTIONARY_v1.2.md`
- 数据模型 → `docs/05_DATA_MODEL_v2.1.md`
- 角色权限 → `docs/07_ROLE_MATRIX_v2.0.md`
- 前端规范 → `docs/10_MINIPROGRAM_SPEC_v2.0.md`
- 插件规范 → `docs/09_PLUGIN_SPEC_v1.1.md`

### 4. 前端规范补全
`docs/10_MINIPROGRAM_SPEC_v2.0.md` 已补齐：
- 开发环境与版本基线
- 常用命令
- 联调依赖
- 前端目录结构
- 前端模块边界与页面路由清单（对齐 `src/app.config.ts`）

---

## 三、当前已知关键问题（必须继续处理）

### 1. API 契约与代码对齐结果（2026-02-05）
已完成后端路由清单与前端调用清单核对：前端调用与 API 契约/后端实现 **无缺失冲突**。

**后端缺失（契约已标注未实现）**：
- 佣金打款接口未实现（后端无路由，契约已标注未实现）。
- 购物卡重置 PIN / 查看 PIN 接口未实现（后端无路由，契约已标注未实现）。
- 手机号验证码登录为三期预留路径（契约标注预留，后端未实现）。

**已知兼容差异**：
- `/gift-cards/mine` 未注册，当前由 `/gift-cards` 提供（契约已标注）。

### 2. 需求明确但尚未实现/仅部分实现
- 优惠组合套装（二期新增）当前前后端均未实现（接口与页面缺失）：参见 [docs/01_功能规格及技术架构说明书_v1.0.md](docs/01_%E5%8A%9F%E8%83%BD%E8%A7%84%E6%A0%BC%E5%8F%8A%E6%8A%80%E6%9C%AF%E6%9E%B6%E6%9E%84%E8%AF%B4%E6%98%8E%E4%B9%A6_v1.0.md#L125-L170)
- 佣金打款接口未实现（后端无路由）：参见 [docs/01_功能规格及技术架构说明书_v1.0.md](docs/01_%E5%8A%9F%E8%83%BD%E8%A7%84%E6%A0%BC%E5%8F%8A%E6%8A%80%E6%9C%AF%E6%9E%B6%E6%9E%84%E8%AF%B4%E6%98%8E%E4%B9%A6_v1.0.md#L256) 与 API 契约。
- 购物卡分享“PDF”仅提供 `print_template_url`，未生成 PDF 文件（标记为部分实现）：参见 [docs/01_功能规格及技术架构说明书_v1.0.md](docs/01_%E5%8A%9F%E8%83%BD%E8%A7%84%E6%A0%BC%E5%8F%8A%E6%8A%80%E6%9C%AF%E6%9E%B6%E6%9E%84%E8%AF%B4%E6%98%8E%E4%B9%A6_v1.0.md#L380)
- 配置规则/任务相关接口未实现（文档仅保留需求描述）：参见 [docs/01_功能规格及技术架构说明书_v1.0.md](docs/01_%E5%8A%9F%E8%83%BD%E8%A7%84%E6%A0%BC%E5%8F%8A%E6%8A%80%E6%9C%AF%E6%9E%B6%E6%9E%84%E8%AF%B4%E6%98%8E%E4%B9%A6_v1.0.md#L690-L700)

---

## 四、前端代码关键变更（已完成）

### 1. 支付流程全面改为微信支付（V2.3.1）
**前端变更：**
- 移除扫码支付选项，统一使用微信支付
- 订单创建后直接跳转支付页（不再跳订单列表）
- 支付页使用 `navigateTo` 保留返回栈
- 支付页显示积分奖励提示

**后端同步变更：**
- `payment-controller.php`: 移除 `offline` 支付方式，仅支持 `wechat`
- `order-controller.php`: 
  - 订单详情移除扫码支付与凭证相关字段（见 API 契约）
  - 新增积分奖励字段（见 API 契约）
  - 注释掉上传支付凭证相关接口（见 API 契约）
- `myshop-core.php`: 禁用 `payment-proof-manager.php` 的加载

**文档更新：**
- `08_API_CONTRACT_V2.3.md` 更新到 V2.3.1，移除扫码支付和上传凭证相关接口
- `10_MINIPROGRAM_SPEC_v2.0.md` 更新支付流程说明

**WordPress后台清理：**
- 禁用"付款凭证"管理菜单
- 订单列表移除"付款凭证"列
- 所有支付通过微信支付回调自动更新订单状态

### 2. `src/services/api.ts`
- 新增 401 处理 `handleUnauthorized`，避免重复跳转。
- 增强 `request` 方法参数（`showLoading`、`suppressErrorToast`、`suppressLog`）。
- 支持开发/测试/正式环境自动计算 API base。
- 新增多个 service 方法（购物卡、优惠券、积分、代理等）。
- 所有请求统一使用 `API_ENDPOINTS`。- **环境变量说明**：`miniEnvVersion`、`effectiveBase`、`nodeEnv` 用于调试时确认小程序连接的服务器环境
### 2. `src/utils/constants.ts`
- 更新 `DEV_BASE_URL` / `PROD_BASE_URL`。
- 新增多个 API 端点常量（积分/代理/分享/优惠券等）。

### 3. `src/app.config.ts`
- 页面路由补全，已对齐实际 `src/pages/` 目录。
- TabBar 设置修正。

### 4. 业务页面实现要点
- `src/pages/order/create.tsx`：积分抵扣逻辑已实现。
- `src/pages/order/payment.tsx`：优惠券与购物卡支付应用已实现。
- `src/pages/shopping-card/share.tsx`、`share-setup.tsx`：分享样式获取与渲染已实现。
- `src/pages/agent/dashboard.tsx`：代理数据拉取已实现。

### 5. 购物卡分享/领取链路修复（新增）
**前端变更：**
- `src/pages/shopping-card/share.tsx` / `share-result.tsx`：分享路径统一为 `/pages/shopping-card/claim?token=...`，分享按钮日志与回调日志补齐。
- `src/pages/shopping-card/claim.tsx`：领取页文案明确“购物卡有效期”，补充说明文本并优化领取成功弹窗逻辑。
- `src/pages/shopping-card/claim.scss`：安全区 padding、行高、文本块级布局优化。
- `src/pages/shopping-card/manage.tsx`：查看订单跳转统一 `/pages/order/detail?orderId=...`，无订单提示修正。
- 新增 `src/pages/shopping-card/share.config.ts` / `share-result.config.ts`：启用 `enableShareAppMessage`。

**后端同步变更：**
- `gift-card-controller.php`：新增 `myshop-giftcard.log` 日志记录与 `/debug/client-log` 端点。
- `order-controller.php`：订单详情允许礼品卡兑换人访问（redeemer_id 校验）。

---

## 五、当前待办清单（必须在新对话继续）

### 1. 核对 API 契约缺失清单（最高优先级）
目标：确认“前端调用但契约缺失”的接口是否存在后端实现。  
需要操作：
- 读取后端仓库/插件代码（或切换工作区）。
- 搜索 REST API 路由注册与控制器实现。
- 输出“已实现 / 未实现 / 仅部分实现”的对照清单。

### 2. 核对功能需求完成度
目标：确认当前代码是否已覆盖 `docs/01_功能规格及技术架构说明书_v1.0.md` 的全部功能。  
需要操作：
- 对照功能规格 → 前端页面/功能 → API 依赖。
- 标记“功能缺失/需降级/需补齐”的项。

### 3. 真机测试准备
目标：确保 API 与前端功能闭环可测。  
前置条件：
- 契约一致性修复完成。
- 缺失功能有明确补齐计划或降级说明。

---

## 六、已知用户偏好/规则（必须遵守）

- 所有回复必须使用中文。
- 若新增 Python 文件：必须包含文件头部 Markdown 模板（模块说明/使用示例）。
- Python 3.11+，函数必须有 type hint，4 空格缩进，单引号字符串。
- 禁止 `print`，必须使用 `logging.getLogger(__name__)`。
- 每个公共函数需 try/except 并抛出自定义 `MyError`。
- 新增功能必须配套 pytest 单元测试，放在 `tests/`。

---

## 七、当前工作区与环境

- 工作区路径：`E:\projects\agri-ecommerce-miniprogram`
- 当前系统：Windows 10 / PowerShell 7
- 前端为 Taro 4 + React 18 小程序
- 后端为 WordPress + WooCommerce + 自定义 REST API 插件
- 对话切换会丢失上下文，因此需要此冻结文档

---

## 八、新对话恢复指令（你可以直接粘贴给新 AI）

“请根据 `docs/工作状态冻结文档.md` 恢复上下文，并优先完成：
1）核对前端调用但契约缺失接口，确认后端实现；
2）对照功能规格确认当前代码完成度；
3）给出真机测试准备建议。”
